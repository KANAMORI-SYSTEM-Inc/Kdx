<Window x:Class="KdxDesigner.Views.Memory.MemorySettingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KdxDesigner.Views" xmlns:utils="clr-namespace:KdxDesigner.Utils"
        mc:Ignorable="d"
        Title="メモリ設定" Height="700" Width="900"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <utils:BoolToColorConverter x:Key="BoolToColorConverter"/>

        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5,2"/>
            <Setter Property="Padding" Value="3"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="10,2"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="5,2"/>
        </Style>


    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- プロファイル選択エリア -->
        <GroupBox Header="プロファイル選択" Grid.Row="0" Padding="10" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- PLC用プロファイル選択 (左列) -->
                <Border Grid.Column="0" BorderBrush="#2196F3" BorderThickness="2" Padding="10" Margin="0,0,5,0" CornerRadius="5">
                    <StackPanel>
                        <TextBlock Text="PLC用プロファイル" FontWeight="Bold" FontSize="14" Foreground="#2196F3" Margin="0,0,0,10"/>
                        <TextBlock Text="Cylinder/Error/Timer/工程時間デバイス設定" FontSize="10" Foreground="Gray" Margin="0,0,0,5"/>
                        <TextBlock Text="※PLC全体に1回のみ適用" FontSize="10" FontStyle="Italic" Foreground="DarkOrange" Margin="0,0,0,10"/>

                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="プロファイル:" Width="90" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <ComboBox ItemsSource="{Binding PlcProfiles}"
                                     SelectedItem="{Binding SelectedPlcProfile}"
                                     DisplayMemberPath="Name"
                                     Width="250" Margin="0,0,0,5">
                                <ComboBox.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="編集" Command="{Binding EditPlcProfileCommand}">
                                            <MenuItem.Icon>
                                                <TextBlock Text="✏️" FontSize="14"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                        <MenuItem Header="削除" Command="{Binding DeletePlcProfileCommand}">
                                            <MenuItem.Icon>
                                                <TextBlock Text="🗑️" FontSize="14"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                    </ContextMenu>
                                </ComboBox.ContextMenu>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                            <Button Content="新規作成" Command="{Binding CreateNewPlcProfileCommand}"
                                    Width="80" Padding="5"
                                    Background="#2196F3" Foreground="White" FontSize="11"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Cycle用プロファイル選択 (右列) -->
                <Border Grid.Column="1" BorderBrush="#4CAF50" BorderThickness="2" Padding="10" Margin="5,0,0,0" CornerRadius="5">
                    <StackPanel>
                        <TextBlock Text="Cycle用プロファイル" FontWeight="Bold" FontSize="14" Foreground="#4CAF50" Margin="0,0,0,10"/>
                        <TextBlock Text="Process/ProcessDetail/Operationデバイス設定" FontSize="10" Foreground="Gray" Margin="0,0,0,5"/>
                        <TextBlock Text="※Cycleごとに複数選択可能" FontSize="10" FontStyle="Italic" Foreground="DarkOrange" Margin="0,0,0,10"/>

                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="プロファイル:" Width="90" VerticalAlignment="Top" Margin="0,5,5,0"/>
                            <ListBox ItemsSource="{Binding CycleProfiles}"
                                     x:Name="CycleProfileListBox"
                                     SelectionMode="Multiple"
                                     DisplayMemberPath="Name"
                                     Width="250" Height="100" Margin="0,0,0,5"
                                     SelectionChanged="ProfileListBox_SelectionChanged">
                                <ListBox.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="編集" Command="{Binding EditCycleProfileCommand}">
                                            <MenuItem.Icon>
                                                <TextBlock Text="✏️" FontSize="14"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                        <MenuItem Header="削除" Command="{Binding DeleteCycleProfileCommand}">
                                            <MenuItem.Icon>
                                                <TextBlock Text="🗑️" FontSize="14"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                    </ContextMenu>
                                </ListBox.ContextMenu>
                                <ListBox.ItemContainerStyle>
                                    <Style TargetType="ListBoxItem">
                                        <Setter Property="Padding" Value="5,2"/>
                                    </Style>
                                </ListBox.ItemContainerStyle>
                            </ListBox>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                            <Button Content="新規作成" Command="{Binding CreateNewCycleProfileCommand}"
                                    Width="80" Padding="5"
                                    Background="#4CAF50" Foreground="White" FontSize="11"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
        </GroupBox>

        <!-- デバイス設定エリア -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <GroupBox Header="デバイス開始番号設定" Padding="10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- 左列: PLC用デバイス -->
                    <Border Grid.Column="0" BorderBrush="#2196F3" BorderThickness="2" Padding="10" Margin="0,0,5,0" CornerRadius="5">
                        <StackPanel>
                            <TextBlock Text="PLC用デバイス (PLC全体に1回適用)" FontWeight="Bold" FontSize="14" Foreground="#2196F3" Margin="0,0,0,10"/>

                            <!-- シリンダデバイス -->
                            <Border BorderBrush="LightGray" BorderThickness="1" Padding="10" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="シリンダデバイス" FontWeight="Bold" FontSize="12" Margin="0,0,0,5"/>

                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Mデバイス [シリンダ]" Width="160"/>
                                        <TextBox Text="{Binding CylinderDeviceStartM}" Width="100"/>
                                        <CheckBox Content="メモリ保存" IsChecked="{Binding IsCylinderMemory}"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Dデバイス [速度変化指令]" Width="160"/>
                                        <TextBox Text="{Binding CylinderDeviceStartD}" Width="100"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <!-- エラーデバイス -->
                            <Border BorderBrush="LightGray" BorderThickness="1" Padding="10" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="エラーデバイス" FontWeight="Bold" FontSize="12" Margin="0,0,0,5"/>

                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Mデバイス [エラー]" Width="160"/>
                                        <TextBox Text="{Binding ErrorDeviceStartM}" Width="100"/>
                                        <CheckBox Content="メモリ保存" IsChecked="{Binding IsErrorMemory}"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Tデバイス [エラー]" Width="160"/>
                                        <TextBox Text="{Binding ErrorDeviceStartT}" Width="100"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="エラー開始番号" Width="160"/>
                                        <TextBox Text="{Binding ErrorStartNum}" Width="100"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <!-- タイマーデバイス -->
                            <Border BorderBrush="LightGray" BorderThickness="1" Padding="10" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="タイマーデバイス" FontWeight="Bold" FontSize="12" Margin="0,0,0,5"/>

                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Tデバイス [タイマ]" Width="160"/>
                                        <TextBox Text="{Binding DeviceStartT}" Width="100"/>
                                        <CheckBox Content="メモリ保存" IsChecked="{Binding IsTimerMemory}"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="ZR [タイマ]" Width="160"/>
                                        <TextBox Text="{Binding TimerStartZR}" Width="100"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <!-- 工程時間デバイス -->
                            <Border BorderBrush="LightGray" BorderThickness="1" Padding="10" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="工程時間デバイス" FontWeight="Bold" FontSize="12" Margin="0,0,0,5"/>

                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="ZR [工程タイム現在]" Width="160"/>
                                        <TextBox Text="{Binding ProsTimeStartZR}" Width="100"/>
                                        <CheckBox Content="メモリ保存" IsChecked="{Binding IsProsTimeMemory}"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="ZR [工程タイム前回]" Width="160"/>
                                        <TextBox Text="{Binding ProsTimePreviousStartZR}" Width="100"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="ZR [CYタイム]" Width="160"/>
                                        <TextBox Text="{Binding CyTimeStartZR}" Width="100"/>
                                        <CheckBox Content="メモリ保存" IsChecked="{Binding IsCyTimeMemory}"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <!-- インターロックデバイス -->
                            <Border BorderBrush="LightGray" BorderThickness="1" Padding="10">
                                <StackPanel>
                                    <TextBlock Text="インターロックデバイス" FontWeight="Bold" FontSize="12" Margin="0,0,0,5"/>

                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Mデバイス [インターロック]" Width="160"/>
                                        <TextBox Text="{Binding InterlockDeviceStartM}" Width="100"/>
                                        <CheckBox Content="メモリ保存" IsChecked="{Binding IsInterlockMemory}"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="インターロック開始番号" Width="160"/>
                                        <TextBox Text="{Binding InterlockStartNum}" Width="100"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <!-- PLC用プロファイル適用ボタン -->
                            <Button Content="プロファイルから適用" Command="{Binding ApplyPlcProfileCommand}"
                                    Width="150" Padding="8" Margin="0,15,0,0"
                                    Background="#FF9800" Foreground="White" FontSize="12" FontWeight="Bold"
                                    HorizontalAlignment="Left"
                                    ToolTip="選択したPLC用プロファイルの設定値を上記のフィールドに適用します"/>
                        </StackPanel>
                    </Border>

                    <!-- 右列: Cycle用デバイス -->
                    <Border Grid.Column="1" BorderBrush="#4CAF50" BorderThickness="2" Padding="10" Margin="5,0,0,0" CornerRadius="5">
                        <StackPanel>
                            <TextBlock Text="Cycle用デバイス (Cycleごとに適用)" FontWeight="Bold" FontSize="14" Foreground="#4CAF50" Margin="0,0,0,10"/>

                            <!-- メモリ保存フラグ（共通設定） -->
                            <Border BorderBrush="LightGray" BorderThickness="1" Padding="10" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="メモリ保存設定（共通）" FontWeight="Bold" FontSize="12" Margin="0,0,0,5"/>
                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox Content="工程" IsChecked="{Binding IsProcessMemory}" Margin="0,0,15,0"/>
                                        <CheckBox Content="工程詳細" IsChecked="{Binding IsDetailMemory}" Margin="0,0,15,0"/>
                                        <CheckBox Content="操作" IsChecked="{Binding IsOperationMemory}"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <!-- 選択されたCycleプロファイル一覧 -->
                            <TextBlock Text="選択されたCycleプロファイル" FontWeight="Bold" FontSize="12" Margin="0,0,0,5"/>
                            <TextBlock Text="※各プロファイルの設定値を個別に編集できます" FontSize="10" Foreground="Gray" Margin="0,0,0,5"/>

                            <ScrollViewer MaxHeight="300" VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding SelectedCycleProfiles}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border BorderBrush="#4CAF50" BorderThickness="1" Padding="10" Margin="0,0,0,10" CornerRadius="3">
                                                <StackPanel>
                                                    <!-- プロファイル名とCycle名 -->
                                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                                        <TextBlock Text="📁" FontSize="14" Margin="0,0,5,0"/>
                                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="12" Foreground="#4CAF50"/>
                                                        <TextBlock Text=" (CycleId: " Foreground="Gray" FontSize="10" VerticalAlignment="Center" Margin="10,0,0,0"/>
                                                        <TextBlock Text="{Binding CycleId}" Foreground="Gray" FontSize="10" VerticalAlignment="Center"/>
                                                        <TextBlock Text=")" Foreground="Gray" FontSize="10" VerticalAlignment="Center"/>
                                                    </StackPanel>

                                                    <!-- デバイス設定 -->
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="80"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="80"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="80"/>
                                                        </Grid.ColumnDefinitions>

                                                        <TextBlock Grid.Column="0" Text="L[工程]:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                                        <TextBox Grid.Column="1" Text="{Binding ProcessDeviceStartL, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                 Width="70" Margin="0,0,10,0"/>

                                                        <TextBlock Grid.Column="2" Text="L[詳細]:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                                        <TextBox Grid.Column="3" Text="{Binding DetailDeviceStartL, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                 Width="70" Margin="0,0,10,0"/>

                                                        <TextBlock Grid.Column="4" Text="M[操作]:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                                        <TextBox Grid.Column="5" Text="{Binding OperationDeviceStartM, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                 Width="70"/>
                                                    </Grid>
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>

                            <!-- プロファイルが選択されていない場合のメッセージ -->
                            <TextBlock Text="Cycleプロファイルを選択してください"
                                       Foreground="Gray" FontStyle="Italic"
                                       HorizontalAlignment="Center" Margin="0,20,0,0"
                                       Visibility="{Binding HasNoCycleProfileSelected, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                            <!-- Cycle用プロファイル適用ボタン -->
                            <Button Content="プロファイルから適用" Command="{Binding ApplyCycleProfileCommand}"
                                    Width="150" Padding="8" Margin="0,15,0,0"
                                    Background="#FF9800" Foreground="White" FontSize="12" FontWeight="Bold"
                                    HorizontalAlignment="Left"
                                    ToolTip="選択したCycle用プロファイルの設定値を上記のフィールドに適用します"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </GroupBox>
        </ScrollViewer>

        <!-- 実行ボタンエリア -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="メモリ設定実行" Command="{Binding ExecuteMemorySettingCommand}"
                    Width="150" Height="35" Margin="0,0,10,0"
                    Background="#2196F3" Foreground="White" FontWeight="Bold" FontSize="14"/>
            <Button Content="閉じる" IsCancel="True"
                    Width="100" Height="35"
                    Padding="5"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,0">
            <TextBlock Text="デバイス数:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBlock Text="{Binding TotalMemoryDeviceCount}" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBlock Text="{Binding LastMemoryConfigTime}" VerticalAlignment="Center" Foreground="DarkGray" 
Visibility="{Binding IsMemoryConfigured, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0,0,10,0"/>

            <TextBlock Text="メモリ設定状態:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"/>

            <Border BorderBrush="{Binding IsMemoryConfigured, 
                        Converter={StaticResource BoolToColorConverter}, 
                        ConverterParameter='Green|Gray'}" BorderThickness="1" CornerRadius="3" Padding="5,2">
                <Border.Style>
                    <Style TargetType="Border">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsMemoryConfigured}" Value="True">
                                <Setter Property="Background" Value="#E6FFE6"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsMemoryConfigured}" Value="False">
                                <Setter Property="Background" Value="#F0F0F0"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <StackPanel Orientation="Horizontal">
                    <Ellipse Width="8" Height="8" Margin="0,0,5,0">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsMemoryConfigured}" Value="True">
                                        <Setter Property="Fill" Value="Green"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsMemoryConfigured}" Value="False">
                                        <Setter Property="Fill" Value="Gray"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock Text="{Binding MemoryConfigurationStatus}" VerticalAlignment="Center"/>
                </StackPanel>

            </Border>
            <!-- メモリデバイス一覧表示ボタン -->
            <Button Command="{Binding ShowMemoryDeviceListCommand}" 
        IsEnabled="{Binding IsMemoryConfigured}"
        Padding="10,3" Margin="10,0,0,0">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📋" FontSize="14" Margin="0,0,5,0"/>
                    <TextBlock Text="一覧表示" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</Window>
