<Window x:Class="KdxDesigner.Views.Tools.CylinderManagement.CylinderManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:KdxDesigner.Controls"
        mc:Ignorable="d"
        Title="シリンダー管理" Height="600" Width="1000"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- タイトル -->
        <TextBlock Grid.Row="0" Text="シリンダー管理" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- シリンダー一覧（共通コンポーネント） -->
        <controls:CylinderListControl Grid.Row="1"
            ItemsSource="{Binding FilteredCylinders}"
            SelectedItem="{Binding SelectedCylinder, Mode=TwoWay}"
            SearchText="{Binding CylinderSearchText, Mode=TwoWay}"
            AddCommand="{Binding AddCylinderCommand}"
            CopyCommand="{Binding CopyCylinderCommand}"
            EditCommand="{Binding EditCylinderCommand}"
            DeleteCommand="{Binding DeleteCylinderCommand}"
            RefreshCommand="{Binding RefreshCommand}"
            ClearSearchCommand="{Binding ClearCylinderSearchCommand}"
            DoubleClickCommand="{Binding EditCylinderCommand}"
            ShowToolbar="True"
            ShowAddButton="True"
            ShowCopyButton="True"
            ShowEditButton="True"
            ShowDeleteButton="True"
            ShowRefreshButton="True"
            ShowSearchBox="True"/>
    </Grid>
</Window>
