<Window x:Class="KdxDesigner.Views.Interlock.InterlockPreConditionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="インターロック前提条件設定" Height="950" Width="1200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="LightGray" Padding="10">
            <TextBlock FontSize="16" FontWeight="Bold" Text="インターロック前提条件設定"/>
        </Border>

        <!-- PreCondition1 Section (コンパクト化) -->
        <GroupBox Grid.Row="1" Header="前提条件1 (InterlockPrecondition1)" Margin="10" Height="150">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <!-- PreCondition1 List -->
                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <ToolBar Grid.Row="0">
                        <Button Content="新規" Command="{Binding AddPreCondition1Command}"/>
                        <Button Content="削除" Command="{Binding DeletePreCondition1Command}"/>
                    </ToolBar>

                    <DataGrid Grid.Row="1"
                              ItemsSource="{Binding PreCondition1List}"
                              SelectedItem="{Binding SelectedPreCondition1}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" IsReadOnly="True"/>
                            <DataGridTextColumn Header="条件名" Binding="{Binding ConditionName}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>

                <!-- PreCondition1 Details -->
                <Grid Grid.Column="1" Margin="10,0,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="条件名:" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding SelectedPreCondition1.ConditionName}" Margin="5,2"/>
                        <CheckBox Grid.Column="2" Content="この条件を選択"
                                  IsChecked="{Binding IsPreCondition1Selected}"
                                  Margin="10,0,0,0" FontWeight="Bold" VerticalAlignment="Center"/>
                    </Grid>

                    <Grid Grid.Row="1" Margin="0,5,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="説明:" VerticalAlignment="Top"/>
                        <TextBox Grid.Column="1" Text="{Binding SelectedPreCondition1.Description}"
                                 TextWrapping="Wrap" AcceptsReturn="True" MinHeight="40" Margin="5,2"/>
                    </Grid>
                </Grid>
            </Grid>
        </GroupBox>

        <!-- PreCondition2 Section (メイン編集エリア) -->
        <GroupBox Grid.Row="2" Header="前提条件2 (InterlockPrecondition2) - ProcessDetail選択" Margin="10,0,10,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="250"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- PreCondition2 List -->
                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <ToolBar Grid.Row="0">
                        <Button Content="新規" Command="{Binding AddPreCondition2Command}"/>
                        <Button Content="削除" Command="{Binding DeletePreCondition2Command}"/>
                    </ToolBar>

                    <DataGrid Grid.Row="1"
                              ItemsSource="{Binding PreCondition2List}"
                              SelectedItem="{Binding SelectedPreCondition2}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" IsReadOnly="True"/>
                            <DataGridTextColumn Header="モード" Binding="{Binding InterlockMode}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>

                <!-- PreCondition2 Details + ProcessDetail選択 -->
                <Grid Grid.Column="1" Margin="10,0,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- 基本設定 -->
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <CheckBox Grid.Column="0" Content="プロセス有効"
                                  IsChecked="{Binding SelectedPreCondition2.IsEnableProcess}"
                                  Margin="0,5"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="20,0,0,0">
                            <TextBlock Text="インターロックモード:" VerticalAlignment="Center"/>
                            <TextBox Text="{Binding SelectedPreCondition2.InterlockMode}" Width="100" Margin="5,2"/>
                        </StackPanel>
                        <CheckBox Grid.Column="2" Content="この条件を選択"
                                  IsChecked="{Binding IsPreCondition2Selected}"
                                  Margin="10,0,0,0" FontWeight="Bold" VerticalAlignment="Center"/>
                    </Grid>

                    <!-- ProcessDetail選択エリア -->
                    <Grid Grid.Row="1" Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- 開始詳細ID選択 -->
                        <GroupBox Grid.Column="0" Header="開始詳細ID (StartDetailId)" Margin="0,0,5,0">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="150"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
                                    <TextBlock Text="選択中: " VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding SelectedStartProcessDetail.Id, StringFormat='ID: {0}', FallbackValue='未選択'}"
                                               FontWeight="Bold" VerticalAlignment="Center"/>
                                    <TextBlock Text=" - " VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding SelectedStartProcessDetail.DetailName, FallbackValue=''}"
                                               VerticalAlignment="Center"/>
                                    <Button Content="クリア" Command="{Binding ClearStartDetailCommand}"
                                            Margin="10,0,0,0" Padding="5,2"/>
                                </StackPanel>

                                <DataGrid Grid.Row="1"
                                          ItemsSource="{Binding ProcessDetails}"
                                          SelectedItem="{Binding SelectedStartProcessDetail}"
                                          AutoGenerateColumns="False"
                                          CanUserAddRows="False"
                                          IsReadOnly="True"
                                          SelectionMode="Single">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                                        <DataGridTextColumn Header="名称" Binding="{Binding DetailName}" Width="*"/>
                                        <DataGridTextColumn Header="CycleId" Binding="{Binding CycleId}" Width="60"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </GroupBox>

                        <!-- 終了詳細ID選択 -->
                        <GroupBox Grid.Column="1" Header="終了詳細ID (EndDetailId)" Margin="5,0,0,0">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="150"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
                                    <TextBlock Text="選択中: " VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding SelectedEndProcessDetail.Id, StringFormat='ID: {0}', FallbackValue='未選択'}"
                                               FontWeight="Bold" VerticalAlignment="Center"/>
                                    <TextBlock Text=" - " VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding SelectedEndProcessDetail.DetailName, FallbackValue=''}"
                                               VerticalAlignment="Center"/>
                                    <Button Content="クリア" Command="{Binding ClearEndDetailCommand}"
                                            Margin="10,0,0,0" Padding="5,2"/>
                                </StackPanel>

                                <DataGrid Grid.Row="1"
                                          ItemsSource="{Binding ProcessDetails}"
                                          SelectedItem="{Binding SelectedEndProcessDetail}"
                                          AutoGenerateColumns="False"
                                          CanUserAddRows="False"
                                          IsReadOnly="True"
                                          SelectionMode="Single">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                                        <DataGridTextColumn Header="名称" Binding="{Binding DetailName}" Width="*"/>
                                        <DataGridTextColumn Header="CycleId" Binding="{Binding CycleId}" Width="60"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </GroupBox>
                    </Grid>

                    <!-- 現在の設定値表示 -->
                    <Border Grid.Row="2" Background="#F5F5F5" Padding="10" Margin="0,10,0,0" CornerRadius="3">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="現在の設定: " FontWeight="Bold"/>
                            <TextBlock Text="StartDetailId = "/>
                            <TextBlock Text="{Binding SelectedPreCondition2.StartDetailId, FallbackValue='null'}" FontWeight="Bold" Foreground="Blue"/>
                            <TextBlock Text="  |  EndDetailId = " Margin="20,0,0,0"/>
                            <TextBlock Text="{Binding SelectedPreCondition2.EndDetailId, FallbackValue='null'}" FontWeight="Bold" Foreground="Blue"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>
        </GroupBox>

        <!-- PreCondition3 Section (IO/デバイスON条件) -->
        <GroupBox Grid.Row="3" Header="前提条件3 (InterlockPrecondition3) - IO/デバイスON条件" Margin="10,0,10,10" Height="150">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- PreCondition3 List -->
                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <ToolBar Grid.Row="0">
                        <Button Content="新規" Command="{Binding AddPreCondition3Command}"/>
                        <Button Content="削除" Command="{Binding DeletePreCondition3Command}"/>
                    </ToolBar>

                    <DataGrid Grid.Row="1"
                              ItemsSource="{Binding PreCondition3List}"
                              SelectedItem="{Binding SelectedPreCondition3}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="40" IsReadOnly="True"/>
                            <DataGridTextColumn Header="タイプ" Binding="{Binding ConditionType}" Width="50"/>
                            <DataGridTextColumn Header="IOアドレス" Binding="{Binding IOAddress}" Width="80"/>
                            <DataGridTextColumn Header="説明" Binding="{Binding Description}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>

                <!-- PreCondition3 Details -->
                <Grid Grid.Column="1" Margin="10,0,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="条件タイプ:" VerticalAlignment="Center"/>
                        <ComboBox Grid.Column="1" SelectedItem="{Binding SelectedPreCondition3.ConditionType}" Margin="5,2">
                            <ComboBoxItem Content="IO"/>
                            <ComboBoxItem Content="Device"/>
                        </ComboBox>
                        <CheckBox Grid.Column="2" Content="ON条件" Margin="15,0,0,0"
                                  IsChecked="{Binding SelectedPreCondition3.IsOnCondition}"
                                  VerticalAlignment="Center"/>
                        <CheckBox Grid.Column="4" Content="この条件を選択"
                                  IsChecked="{Binding IsPreCondition3Selected}"
                                  Margin="10,0,0,0" FontWeight="Bold" VerticalAlignment="Center"/>
                    </Grid>

                    <Grid Grid.Row="1" Margin="0,5,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="IOアドレス:" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding SelectedPreCondition3.IOAddress}" Margin="5,2"/>
                        <Button Grid.Column="2" Content="IO検索" Command="{Binding SearchIOCommand}"
                                Margin="5,2" Padding="10,2"/>
                        <TextBlock Grid.Column="3" Text="デバイス:" VerticalAlignment="Center" Margin="10,0,0,0"/>
                        <TextBox Grid.Column="4" Text="{Binding SelectedPreCondition3.DeviceAddress}" Margin="5,2"/>
                    </Grid>

                    <Grid Grid.Row="2" Margin="0,5,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="説明:" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding SelectedPreCondition3.Description}" Margin="5,2"/>
                    </Grid>
                </Grid>
            </Grid>
        </GroupBox>

        <!-- Bottom Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="保存" Width="100" Height="30" Margin="5" Command="{Binding SaveCommand}"/>
            <Button Content="キャンセル" Width="100" Height="30" Margin="5" Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</Window>